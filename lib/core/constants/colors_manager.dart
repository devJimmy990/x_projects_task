import 'package:flutter/material.dart';

class ColorsManager {
  static const Color black = Color.fromRGBO(0, 0, 0, 1);
  static const Color grey = Color.fromRGBO(20, 30, 40, .04);
  static const Color white = Color.fromRGBO(255, 255, 255, 1);
  static const Color blackPrimary = Color.fromRGBO(20, 30, 40, 1);
  static const Color blackSecondary = Color.fromRGBO(20, 30, 40, 0.48);

  // Optional extended colors for better dark theme contrast
  static const Color greyDark = Color.fromRGBO(200, 200, 200, 0.1);
  static const Color darkSurface = Color.fromRGBO(30, 30, 30, 1);
}
